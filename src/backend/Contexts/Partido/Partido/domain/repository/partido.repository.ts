export interface PartidoRepository {
  // Define repository methods here
  // Example:
  // findById(id: string): Promise<Partido | null>;
  // save(partido: Partido): Promise<void>;
  // delete(id: string): Promise<void>;
}