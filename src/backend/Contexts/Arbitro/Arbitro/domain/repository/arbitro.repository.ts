export interface ArbitroRepository {
  // Define repository methods here
  // Example:
  // findById(id: string): Promise<Arbitro | null>;
  // save(arbitro: Arbitro): Promise<void>;
  // delete(id: string): Promise<void>;
}