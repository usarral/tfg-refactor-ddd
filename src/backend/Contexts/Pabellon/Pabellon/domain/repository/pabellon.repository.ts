export interface PabellonRepository {
  // Define repository methods here
  // Example:
  // findById(id: string): Promise<Pabellon | null>;
  // save(pabellon: Pabellon): Promise<void>;
  // delete(id: string): Promise<void>;
}