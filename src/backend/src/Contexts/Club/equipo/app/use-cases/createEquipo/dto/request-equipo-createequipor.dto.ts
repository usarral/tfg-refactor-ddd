import { Request } from '@shared/app/use-cases/request'
    
export class RequestEquipoCreateequipor implements Request {
  // Add your request DTO properties here and implements Request shared interface
}